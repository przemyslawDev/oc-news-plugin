# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: name
    slug:
        label: slug
    created_at:
        label: created_at
        type: date
    updated_at:
        label: updated_at
        type: date