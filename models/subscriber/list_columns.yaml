# ===================================
#  List Column Definitions
# ===================================

columns:
    email:
        label: email
    is_active:
        label: active
    created_at:
        label: created_at
        type: date
    updated_at:
        label: updated_at
        type: date